name: airline_booking
description: Airline Booking System - Flutter Mobile App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  provider: ^6.1.2
  
  # HTTP & API
  http: ^1.2.1
  dio: ^5.7.0
  web: ^1.0.0
  
  # PDF & Printing
  pdf: ^3.11.1
  printing: ^5.13.2
  
  # Storage
  path_provider: ^2.1.5
  shared_preferences: ^2.3.2

  # JSON
  json_annotation: ^4.9.0
  
  # UI
  flutter_svg: ^2.0.10+1
  cached_network_image: ^3.3.1
  qr_flutter: ^4.1.0
  
  # Date & Time
  intl: ^0.19.0
  
  # Utils
  uuid: ^4.4.0
  flutter_dotenv: ^5.1.0

dependency_overrides:
  path_provider_android: ^2.2.12

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.9
  json_serializable: ^6.8.0

flutter:
  uses-material-design: true
  
  assets:
    - .env
